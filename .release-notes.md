v0.6.15 更新说明

修复：
- [修复] AI 注册的脚本工具收到数组/对象参数时被压平成字符串的问题（如 [3,11,7,5.5] 变 "3,11,7,5.5" 导致逐字符迭代），现在完整保真传递
- [修复] 聊天中行内代码（如 `pie`、`flowchart TD`）被误渲染成 mermaid 图、撑坏表格排版的问题
- [修复] runtime="py"（Windows py 启动器）注册/执行工具时报"解释器未注册"的问题，现在自动解析到已注册的 Python 运行时
- [修复] PowerShell 下裸 & 写法静默丢输出且假成功的问题：现在会提前警告并给出正确写法（; 串联或 cmd /c 包裹）

优化：
- [优化] PowerShell 执行链路增强：&& / ||、含 $ 变量的内联命令、stderr 分流与退出码回传全面可用
- [优化] mermaid 渲染救援逻辑迁移并增强（erDiagram 实体块整段捕获，渲染失败自动回退源码）
- [优化] 新增本机操控工具开关（画图/看图），设置页可独立控制，默认关闭

平台说明：
- Windows：PowerShell 相关修复仅影响 Windows 端；py 启动器别名需本机装有 Python（python/python3/py 任一可探测到即可）
- macOS：shell 默认走用户登录 shell（zsh/fish/bash），不受 PowerShell 语义问题影响；py 别名兜底同样生效（python3 优先）；本机操控（截屏/键鼠）需在系统设置中授予 BIT 辅助功能与屏幕录制权限
- Linux：脚本工具入参修复与平台无关、全端生效

安装 / 更新：
- 老用户：应用内「设置 → 关于」检查更新（GitHub Release → osbt.space 镜像自动同步），或到 Release 页重新下载
- Windows x64：`BIT_0.6.15_x64-setup.exe`（NSIS）/ `.msi` / `BIT_0.6.15_x64-portable.zip` 免安装版
- Windows ARM64：`BIT_0.6.15_aarch64-setup.exe`
- macOS：Apple Silicon 用 `BIT_0.6.15_aarch64.dmg`，Intel 用 `BIT_0.6.15_x64.dmg`；另有免安装 `.app` 便携版 zip
- macOS 首次打开提示：应用为 ad-hoc 签名（未公证），若 Gatekeeper 拦截，右键 → 打开，或终端执行 `xattr -d com.apple.quarantine /Applications/BIT.app`
- Linux：`.deb` / `.AppImage`（x64 + arm64），Gentoo/Void 等用通用二进制 `tar.gz`；龙芯 LoongArch64、RISC-V、ppc64le 由 QEMU 交叉构建（发布时间会稍晚于主流平台）
